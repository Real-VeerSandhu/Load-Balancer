#include "RequestGenerator.h"

int RequestGenerator::generate(int count) {
    // Simple implementation - just returns the count
    // In a more advanced version, this could simulate request patterns
    return count;
}

